import { ChangeTemperaturePipe } from './change-temperature.pipe';

describe('ChangeTemperaturePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeTemperaturePipe();
    expect(pipe).toBeTruthy();
  });
});
